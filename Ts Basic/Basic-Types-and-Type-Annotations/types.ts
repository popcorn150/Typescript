// Defining different types

let username: string = 'David'
let age: number = 20
let isDeveloper: boolean = true

let favoriteLanguages: string[] = ['Javascript', 'Typescript']
let favoriteFoods: string[] = ['Beans', 'Plantain', 'Spaghetti']
let favoriteNumbers: number[] = [7, 10, 15, 20]

let firstChild: [string, number] = ['Alice', 20]
let secondChild: [string, number] = ['Esther', 18]
let thirdChild: [string, number] = ['Alex', 16]

enum Direction {
    Up,
    Down,
    Left, 
    Right
}

let movement: Direction = Direction.Up